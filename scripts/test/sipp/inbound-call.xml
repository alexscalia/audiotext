<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  inbound-call.xml — full INVITE ladder for the audiotext IVR slice.

  Simulates a carrier delivering an inbound call to kamailio-edge:
    INVITE → 100 → 200 → ACK → (media pause) → BYE → 200

  Variables (override with -key on sipp CLI or via -inf CSV):
    [service]       Request-URI user (B-number, may carry tech prefix)
    [from_user]     From user (A-number / caller id)
    [media_ms]      How long to "talk" before hanging up (default 5000)

  Use:
    sipp -sf inbound-call.xml -s 99+18005551234 -m 1 \
         -p 5099 kamailio-edge:5060
-->

<scenario name="audiotext-inbound-call">

  <send retrans="500">
    <![CDATA[

      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "+15555550001" <sip:+15555550001@[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: sip:caller@[local_ip]:[local_port]
      Max-Forwards: 70
      Subject: audiotext inbound test
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[local_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0
      a=rtpmap:0 PCMU/8000

    ]]>
  </send>

  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>
  <recv response="183" optional="true"/>
  <recv response="200" rrs="true"/>

  <send>
    <![CDATA[

      ACK [next_url] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "+15555550001" <sip:+15555550001@[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: sip:caller@[local_ip]:[local_port]
      Max-Forwards: 70
      [routes]
      Content-Length: 0

    ]]>
  </send>

  <!-- Media phase: caller hears IVR. Length controls how many billsec
       seconds the CDR listener will settle. Default 5s = ~5 billsec. -->
  <pause milliseconds="5000"/>

  <send retrans="500">
    <![CDATA[

      BYE [next_url] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "+15555550001" <sip:+15555550001@[local_ip]:[local_port]>;tag=[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Contact: sip:caller@[local_ip]:[local_port]
      Max-Forwards: 70
      [routes]
      Content-Length: 0

    ]]>
  </send>

  <recv response="200" crlf="true"/>

</scenario>
