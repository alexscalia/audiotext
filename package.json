{
  "name": "audiotext",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@11.1.1",
  "engines": {
    "node": ">=25.6.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "db:generate": "pnpm --filter @audiotext/db db:generate",
    "db:migrate": "pnpm --filter @audiotext/db db:migrate",
    "db:studio": "pnpm --filter @audiotext/db db:studio",
    "db:seed": "pnpm --filter @audiotext/db db:seed",
    "db:reset": "pnpm --filter @audiotext/db db:reset",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "3.4.2",
    "turbo": "2.9.12",
    "typescript": "^6.0.3"
  }
}
